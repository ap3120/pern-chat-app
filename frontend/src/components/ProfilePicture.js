import { useState } from "react";
import AccountCircleIcon from '@mui/icons-material/AccountCircle';
import { Avatar } from "@mui/material";


export const ProfilePicture = ({ user_id }) => {

  return (
    <Avatar sx={{marginRight:'20px'}} alt="" src={""} />
  )
}
